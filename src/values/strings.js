const dateFormat = 'DD/MM/YYYY'

const navbar = {
  login: 'LOGIN',
  intimacoes: 'INTIMAÇÕES',
  processos: 'PROCESSOS'
}

const error = {
  cache: {
    set: 'Error on save item in cache',
    get: 'Error on get item from cache',
    remove: 'Error on remove item from cache'
  }
}

const strings = {
  dateFormat,
  navbar,
  error
}

export default strings
