import colors from '../../../../values/colors'
import sizes from '../../../../values/sizes'

const styles = {
  container: { 
    marginBottom: 25
  },
  containerBanner: {
    height: 310,
    width: 300
  },
  containerInfo: {
    opacity: 0.9,
    margin: 12,
    backgroundColor: colors.primary
  },
  dataText: {
    fontSize: sizes.elements.margin.button + 7,
    color: colors.white
  },
  participanteText: {
    color: colors.white
  }
}

export default styles
